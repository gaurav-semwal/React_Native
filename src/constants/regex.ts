export const regExp = {
    strLowerCase: /^(?=.*[a-z])/,
    strUpperCase: /^(?=.*[A-Z])/,
    containsNum: /^(?=.*[0-9])/,
    containsSpecialChar: /^(?=.*[!@#$%^&*])/,
    onlyNumbers: /^\d+$/,
    email: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    phoneRegExp: /^[6-9]\d{9}$/,
  };